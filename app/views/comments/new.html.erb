<div class="clear"></div>
<%= simple_form_for @comment, url: comment_path(@comment) do |f| %>
  <%= f.input :message %>
  <%= f.input :rating %>
  <%= f.submit "Add a Comment", class: 'btn btn-primary' %>
<% end %>